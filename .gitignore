# Dependencies (tools/review; add your stack's below)
node_modules/

# Claude Code local overrides (copy settings.local.json.example to start)
.claude/settings.local.json

# Git worktrees created by scripts/worktree-create.sh
.worktrees/

# CodeGraph index (built by `codegraph init`, per checkout)
.codegraph/

# Build output
dist/
build/

# Env / secrets
.env
.env.*
!.env.example
secrets.env

# OS / editor noise
.DS_Store
*.log
