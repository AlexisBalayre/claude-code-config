{
  "name": "claude-code-power-config",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A complete, opinionated Claude Code configuration for a strict-convention TypeScript monorepo.",
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "worktree:create": "bash scripts/worktree-create.sh",
    "worktree:clean": "bash scripts/worktree-clean.sh",
    "fix-esm-imports": "node scripts/fix-esm-imports.mjs",
    "lint": "echo 'Replace with your linter, e.g. biome check .'",
    "lint:fix": "echo 'Replace with your linter, e.g. biome check --write .'",
    "typecheck": "echo 'Replace with your typechecker, e.g. tsc -b'",
    "test": "echo 'Replace with your test runner, e.g. vitest run'"
  }
}
