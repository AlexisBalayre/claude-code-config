# Acme

@AGENTS.md

## Claude Code specifics

Path-scoped rules in `.claude/rules/*.md` auto-load the matching `docs/conventions/<area>.md` when you touch a file in that area; the conventions table above is how tools without that mechanism find the same rules.
